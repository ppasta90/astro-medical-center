<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="description" content="Astro description" />
    <meta name="viewport" content="width=device-width" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="generator" content={Astro.generator} />
    <title>Centro Medico Agliana</title>
  </head>
  <body>
    <slot />
  </body>
</html>
<style is:global>
  html {
    font-family: "DM Sans", sans-serif;
  }
  .green-cta {
    @apply xl:min-w-[180px] rounded-3xl cursor-pointer font-semibold py-[10px] px-4 border-none bg-primary text-white;
  }
  .blue-cta {
    @apply xl:min-w-[180px] rounded-3xl cursor-pointer font-semibold py-[10px] px-4 border-none bg-secondary text-white;
  }

  .white-cta {
    @apply xl:min-w-[180px] rounded-3xl border-2 border-secondary cursor-pointer font-semibold py-[10px] px-4 bg-white text-secondary;
  }
</style>
