---
interface Props {
  title: string;
  description: string;
}

const { title, description } = Astro.props;
---

<div class="bg-white rounded-md p-[10px] w-full lg:w-[30%]">
  <h2 class="text-lg font-bold mb-2">{title}</h2>
  <p>{description}</p>
</div>
